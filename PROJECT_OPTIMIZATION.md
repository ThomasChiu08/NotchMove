# NotchMove 项目设定与优化文档

## 项目定位

NotchMove 是一款原生 macOS 菜单栏健康提醒工具，核心目标是在长时间使用电脑时，用贴近刘海区域的轻量浮层提醒用户站立、伸展和活动。应用当前采用 SwiftUI + AppKit 架构：SwiftUI 负责设置页和浮层视图，AppKit 负责菜单栏、无边框浮层窗口和系统屏幕信息。

当前项目适合继续保持轻量工具定位，不建议引入复杂的数据层。提醒偏好、统计计数和语言设置用 `UserDefaults` 持久化已经足够；只有当后续需要可查询的历史记录、趋势图或多设备同步时，才需要考虑 SwiftData 或其他持久化层。

## 当前架构评价

- `AppDelegate` 是运行时组合入口，负责创建偏好、统计、语言、活动监控、提醒引擎、菜单栏和浮层窗口控制器，职责清晰。
- `ReminderEngine` 集中管理提醒生命周期，避免多个 UI 组件各自维护提醒状态，这是当前架构最重要的稳定点。
- `ScreenPlacementService` 只做几何计算，并已有单元测试保护，适合继续保持纯服务形态。
- `PreferencesStore` 将偏好变更持久化，并通过定向通知驱动运行时更新，适合当前规模。
- 本地化已覆盖 English、简体中文、繁体中文和日文，适合后续扩展设置项。

主要风险在多屏环境：之前浮层目标屏幕直接依赖 `NSScreen.main`。在外接显示器、菜单栏主屏切换、焦点切换或屏幕参数变化后，macOS 可能改变 main screen，导致 NotchMove 浮层跳到用户不期望的屏幕。

## 已实施的多屏优化

- 新增“浮层显示屏幕”设置，用户可以选择自动或指定某个当前连接的屏幕。
- 使用 `CGDirectDisplayID` 持久化用户选择，避免依赖屏幕顺序或 `NSScreen.main`。
- 自动模式策略调整为：优先内建刘海屏，其次内建屏，其次当前主屏，最后才使用屏幕列表中的第一个屏幕。
- 当用户指定的屏幕暂时不可用时，运行时降级到自动策略，但不清除用户原选择；屏幕重新连接后可恢复到指定屏幕。
- 将屏幕选择拆成纯 `ScreenSelectionService`，与 `ScreenPlacementService` 的几何计算分离，便于单元测试覆盖。

这能解决“笔记本连上屏幕后浮层跳到别的屏幕”的核心问题：浮层不再因为系统 main screen 改变而盲目移动。

## 后续优化建议

- 设置体验：将“浮层显示屏幕”保留在行为区，但后续可增加更明确的说明文案，例如“自动会优先使用内建刘海屏”。
- 诊断能力：为屏幕选择增加轻量日志，记录当前屏幕列表、选择模式、降级原因，方便排查用户多屏反馈。
- 发布流程：补充 Release checklist，包括构建、测试、DMG 打包、签名和 smoke test。
- 多屏 QA：固定测试矩阵，覆盖单内建屏、内建屏 + 外接屏、外接屏设为主屏、合盖模式、拔插外接屏。
- UI 测试：当前 UI tests 还比较基础，后续可以增加设置页打开、语言切换和 Picker 基础交互验证。

## 验收标准

- 只使用笔记本屏幕时，浮层贴合内建屏刘海或顶部区域。
- 连接外接屏后，如果选择内建屏，浮层不会跳到外接屏。
- 如果选择外接屏，浮层固定显示在该外接屏顶部居中。
- 被选择的外接屏拔掉后，浮层临时降级到可用屏幕；重新连接后恢复到该外接屏。
- 切换 macOS 主屏幕或点击不同屏幕上的应用，不会让浮层无故跳屏。
