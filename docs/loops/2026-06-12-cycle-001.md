# Cycle 001 - Loop Baseline

Date: 2026-06-12

## Signal Scan

Inputs reviewed:

- `AGENTS.md`
- `README.md`
- `docs/qa/notchmove-smoke-test.md`
- `docs/plans/2026-06-07-notch-hub-review-fix-plan.md`
- `docs/plans/2026-06-08-lightweight-focus-notch-hub-plan.md`
- `docs/plans/2026-06-03-hover-next-reminders-plan.md`
- Notch Hub store and provider tests

## Scope Gate

Kept in the active loop:

- Automated verification baseline.
- Notch Hub permission and priority issues.
- Overlay motion and hover preview improvements.
- Signed manual smoke test tracking.

Parked:

- Broad AI schedule and voice expansion plans unless they become release
  blockers.
- Any Hub work that turns the notch surface into a general dashboard.

## Findings

- The highest-risk P1 signal, Media refresh bypassing Apple Events permission,
  is already implemented and covered by tests in the current checkout.
- Camera Mirror session work is already off the main thread through a serial
  session queue.
- Hub disabling and active-widget disabling are already covered by tests.
- Production overlay priority already uses `NotchHubPresentationResolver` in
  both the AppKit controller and SwiftUI view.
- Hover next reminder preview is already implemented through
  `ReminderEngine.nextReminderPreview(at:)`, `NextReminderPreviewView`, and
  `OverlaySizingRole.dualPreview`.
- The useful cycle output is establishing a single verification command and
  clearing stale ready items from the loop backlog.

## Decision

Cycle 001 ships loop infrastructure only:

- `docs/loops/README.md`
- `docs/loops/backlog.md`
- `script/loop_verify.sh`

No runtime behavior is changed in this cycle.

## Verification

Run:

```sh
script/loop_verify.sh
```

Record the exact result in the final handoff for this cycle.

Result:

- `git diff --check`: passed
- `plutil -lint`: passed for project, entitlements, and all localization files
- `xcodebuild test`: passed
- Result bundle:
  `/tmp/NotchMove-DerivedData/Logs/Test/Test-NotchMove-2026.06.12_07-48-51-+0800.xcresult`
